services:
  - type: web
    name: youpro-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app -b 0.0.0.0:5000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
